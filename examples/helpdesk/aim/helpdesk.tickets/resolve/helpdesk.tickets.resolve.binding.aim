---
aim: helpdesk.tickets.resolve
kind: binding
parent: helpdesk.tickets
---

# Resolve Bindings

## Bind: Contract:ResolveTicket

- binds: `src/tickets/resolve.ts#resolveTicket` — as: handler
- binds: `route:POST /api/tickets/:id/resolve` — as: route

## Bind: Event:TicketResolved

- binds: `topic:helpdesk.tickets.resolved` — as: topic
